*     File es_esmm/aes_esmmmptbl.f : Sat Mar 18 19:45:02 2000
*
*     Generated by GRACE (Fortran source code generator)
*         Ver.2.1.5(2) 1998/05/11
*
*     (c)copyright 1990-1998 Minami-Tateya Group, Japan
*-----------------------------------------------------------------------
*
************************************************************************
      subroutine aes_esmmmptbl
      implicit real*8(a-h,o-z)
      include 'incles_esmm1.h'
      include 'incl2.h'
      include 'inclk.h'
      include 'incles_esmmp.h'
*-----------------------------------------------------------------------
      jgraph = 0

* External lines
      call smextf(lincom,amdq(2),pfes_esmm2,ptes_esmm2t,cfes_esmm2t)
      exes_esmm2t(1) = lprtcl
      call smextf(loutgo,amlp(1),pfes_esmm4,ptes_esmm4m,cfes_esmm4m)
      exes_esmm4m(1) = lantip
      call smextf(loutgo,amdq(2),pfes_esmm9,ptes_esmm9t,cfes_esmm9t)
      exes_esmm9t(1) = lprtcl
      call smextf(lincom,amlp(1),pfes_esmm17,ptes_esmm17m,cfes_esmm17m)
      exes_esmm17m(1) = lantip
      call smextf(loutgo,amlp(2),pfes_esmm33,ptes_esmm33n,cfes_esmm33n)
      exes_esmm33n(1) = lprtcl
      call smextf(lincom,amlp(2),pfes_esmm62,ptes_esmm62n,cfes_esmm62n)
      exes_esmm62n(1) = lantip


* Buffer clear for amplitude
      do 200 ih = 0 , les_esmmag-1
         agc(ih) = (0.0d0,0.0d0)
  200 continue

      call aes_esmmmpt0

      return
      end
************************************************************************
      subroutine aes_esmmmpt0
      implicit real*8(a-h,o-z)

      include 'incles_esmm1.h'
      include 'incl2.h'
      include 'inclk.h'
*-----------------------------------------------------------------------
      if(jselg(1) .ne. 0) then
        jgraph = jgraph + 1
        igraph(jgraph) = 1
        call aes_esmmg1
      endif

      if(jselg(2) .ne. 0) then
        jgraph = jgraph + 1
        igraph(jgraph) = 2
        call aes_esmmg2
      endif

      if(jselg(3) .ne. 0) then
        jgraph = jgraph + 1
        igraph(jgraph) = 3
        call aes_esmmg3
      endif

      if(jselg(4) .ne. 0) then
        jgraph = jgraph + 1
        igraph(jgraph) = 4
        call aes_esmmg4
      endif

      if(jselg(5) .ne. 0) then
        jgraph = jgraph + 1
        igraph(jgraph) = 5
        call aes_esmmg5
      endif

      if(jselg(6) .ne. 0) then
        jgraph = jgraph + 1
        igraph(jgraph) = 6
        call aes_esmmg6
      endif

      if(jselg(7) .ne. 0) then
        jgraph = jgraph + 1
        igraph(jgraph) = 7
        call aes_esmmg7
      endif

      if(jselg(8) .ne. 0) then
        jgraph = jgraph + 1
        igraph(jgraph) = 8
        call aes_esmmg8
      endif

      if(jselg(9) .ne. 0) then
        jgraph = jgraph + 1
        igraph(jgraph) = 9
        call aes_esmmg9
      endif

      if(jselg(10) .ne. 0) then
        jgraph = jgraph + 1
        igraph(jgraph) = 10
        call aes_esmmg10
      endif

      if(jselg(11) .ne. 0) then
        jgraph = jgraph + 1
        igraph(jgraph) = 11
        call aes_esmmg11
      endif

      if(jselg(12) .ne. 0) then
        jgraph = jgraph + 1
        igraph(jgraph) = 12
        call aes_esmmg12
      endif

      if(jselg(13) .ne. 0) then
        jgraph = jgraph + 1
        igraph(jgraph) = 13
        call aes_esmmg13
      endif

      if(jselg(14) .ne. 0) then
        jgraph = jgraph + 1
        igraph(jgraph) = 14
        call aes_esmmg14
      endif

      if(jselg(15) .ne. 0) then
        jgraph = jgraph + 1
        igraph(jgraph) = 15
        call aes_esmmg15
      endif

      if(jselg(16) .ne. 0) then
        jgraph = jgraph + 1
        igraph(jgraph) = 16
        call aes_esmmg16
      endif

      if(jhiggs .ne. 0) then
      if(jselg(17) .ne. 0) then
        jgraph = jgraph + 1
        igraph(jgraph) = 17
        call aes_esmmg17
      endif
      endif

      if(jselg(18) .ne. 0) then
        jgraph = jgraph + 1
        igraph(jgraph) = 18
        call aes_esmmg18
      endif

      if(jselg(19) .ne. 0) then
        jgraph = jgraph + 1
        igraph(jgraph) = 19
        call aes_esmmg19
      endif

      if(jselg(20) .ne. 0) then
        jgraph = jgraph + 1
        igraph(jgraph) = 20
        call aes_esmmg20
      endif

      if(jselg(21) .ne. 0) then
        jgraph = jgraph + 1
        igraph(jgraph) = 21
        call aes_esmmg21
      endif

      if(jselg(22) .ne. 0) then
        jgraph = jgraph + 1
        igraph(jgraph) = 22
        call aes_esmmg22
      endif

      if(jselg(23) .ne. 0) then
        jgraph = jgraph + 1
        igraph(jgraph) = 23
        call aes_esmmg23
      endif

      if(jselg(24) .ne. 0) then
        jgraph = jgraph + 1
        igraph(jgraph) = 24
        call aes_esmmg24
      endif

      if(jselg(25) .ne. 0) then
        jgraph = jgraph + 1
        igraph(jgraph) = 25
        call aes_esmmg25
      endif

      return
      end
