*     File eub_eubmm/aeub_eubmmmptbl.f : Sat Mar 18 19:44:59 2000
*
*     Generated by GRACE (Fortran source code generator)
*         Ver.2.1.5(2) 1998/05/11
*
*     (c)copyright 1990-1998 Minami-Tateya Group, Japan
*-----------------------------------------------------------------------
*
************************************************************************
      subroutine aeub_eubmmmptbl
      implicit real*8(a-h,o-z)
      include 'incleub_eubmm1.h'
      include 'incl2.h'
      include 'inclk.h'
      include 'incleub_eubmmp.h'
*-----------------------------------------------------------------------
      jgraph = 0

* External lines
      call smextf(loutgo,amuq(1),pfeub_eubmm2,pteub_eubmm2p,cfeub_eubmm2p)
      exeub_eubmm2p(1) = lantip
      call smextf(loutgo,amlp(1),pfeub_eubmm4,pteub_eubmm4m,cfeub_eubmm4m)
      exeub_eubmm4m(1) = lantip
      call smextf(lincom,amuq(1),pfeub_eubmm9,pteub_eubmm9p,cfeub_eubmm9p)
      exeub_eubmm9p(1) = lantip
      call smextf(lincom,amlp(1),pfeub_eubmm17,pteub_eubmm17m,cfeub_eubmm17m)
      exeub_eubmm17m(1) = lantip
      call smextf(loutgo,amlp(2),pfeub_eubmm33,pteub_eubmm33n,cfeub_eubmm33n)
      exeub_eubmm33n(1) = lprtcl
      call smextf(lincom,amlp(2),pfeub_eubmm62,pteub_eubmm62n,cfeub_eubmm62n)
      exeub_eubmm62n(1) = lantip


* Buffer clear for amplitude
      do 200 ih = 0 , leub_eubmmag-1
         agc(ih) = (0.0d0,0.0d0)
  200 continue

      call aeub_eubmmmpt0

      return
      end
************************************************************************
      subroutine aeub_eubmmmpt0
      implicit real*8(a-h,o-z)

      include 'incleub_eubmm1.h'
      include 'incl2.h'
      include 'inclk.h'
*-----------------------------------------------------------------------
      if(jselg(1) .ne. 0) then
        jgraph = jgraph + 1
        igraph(jgraph) = 1
        call aeub_eubmmg1
      endif

      if(jselg(2) .ne. 0) then
        jgraph = jgraph + 1
        igraph(jgraph) = 2
        call aeub_eubmmg2
      endif

      if(jselg(3) .ne. 0) then
        jgraph = jgraph + 1
        igraph(jgraph) = 3
        call aeub_eubmmg3
      endif

      if(jselg(4) .ne. 0) then
        jgraph = jgraph + 1
        igraph(jgraph) = 4
        call aeub_eubmmg4
      endif

      if(jselg(5) .ne. 0) then
        jgraph = jgraph + 1
        igraph(jgraph) = 5
        call aeub_eubmmg5
      endif

      if(jselg(6) .ne. 0) then
        jgraph = jgraph + 1
        igraph(jgraph) = 6
        call aeub_eubmmg6
      endif

      if(jselg(7) .ne. 0) then
        jgraph = jgraph + 1
        igraph(jgraph) = 7
        call aeub_eubmmg7
      endif

      if(jselg(8) .ne. 0) then
        jgraph = jgraph + 1
        igraph(jgraph) = 8
        call aeub_eubmmg8
      endif

      if(jselg(9) .ne. 0) then
        jgraph = jgraph + 1
        igraph(jgraph) = 9
        call aeub_eubmmg9
      endif

      if(jselg(10) .ne. 0) then
        jgraph = jgraph + 1
        igraph(jgraph) = 10
        call aeub_eubmmg10
      endif

      if(jselg(11) .ne. 0) then
        jgraph = jgraph + 1
        igraph(jgraph) = 11
        call aeub_eubmmg11
      endif

      if(jselg(12) .ne. 0) then
        jgraph = jgraph + 1
        igraph(jgraph) = 12
        call aeub_eubmmg12
      endif

      if(jselg(13) .ne. 0) then
        jgraph = jgraph + 1
        igraph(jgraph) = 13
        call aeub_eubmmg13
      endif

      if(jselg(14) .ne. 0) then
        jgraph = jgraph + 1
        igraph(jgraph) = 14
        call aeub_eubmmg14
      endif

      if(jselg(15) .ne. 0) then
        jgraph = jgraph + 1
        igraph(jgraph) = 15
        call aeub_eubmmg15
      endif

      if(jselg(16) .ne. 0) then
        jgraph = jgraph + 1
        igraph(jgraph) = 16
        call aeub_eubmmg16
      endif

      if(jhiggs .ne. 0) then
      if(jselg(17) .ne. 0) then
        jgraph = jgraph + 1
        igraph(jgraph) = 17
        call aeub_eubmmg17
      endif
      endif

      if(jselg(18) .ne. 0) then
        jgraph = jgraph + 1
        igraph(jgraph) = 18
        call aeub_eubmmg18
      endif

      if(jselg(19) .ne. 0) then
        jgraph = jgraph + 1
        igraph(jgraph) = 19
        call aeub_eubmmg19
      endif

      if(jselg(20) .ne. 0) then
        jgraph = jgraph + 1
        igraph(jgraph) = 20
        call aeub_eubmmg20
      endif

      if(jselg(21) .ne. 0) then
        jgraph = jgraph + 1
        igraph(jgraph) = 21
        call aeub_eubmmg21
      endif

      if(jselg(22) .ne. 0) then
        jgraph = jgraph + 1
        igraph(jgraph) = 22
        call aeub_eubmmg22
      endif

      if(jselg(23) .ne. 0) then
        jgraph = jgraph + 1
        igraph(jgraph) = 23
        call aeub_eubmmg23
      endif

      if(jselg(24) .ne. 0) then
        jgraph = jgraph + 1
        igraph(jgraph) = 24
        call aeub_eubmmg24
      endif

      if(jselg(25) .ne. 0) then
        jgraph = jgraph + 1
        igraph(jgraph) = 25
        call aeub_eubmmg25
      endif

      return
      end
